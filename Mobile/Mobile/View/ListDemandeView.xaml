<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
              xmlns:vm="clr-namespace:Mobile.ViewModel"
             xmlns:behaviors="clr-namespace:Mobile.Behaviors"
             x:Class="Mobile.View.ListDemandeView">

    <ContentPage.Resources>
        <ResourceDictionary>
            <vm:DemandeAccesVM x:Key="vm"/>
        </ResourceDictionary>
    </ContentPage.Resources>

    <ContentPage.Content>
        <ListView ItemsSource="{Binding Source={StaticResource vm}, Path=DemandeAcces}">
            <ListView.Behaviors>
                <behaviors:ListViewBehavior/>
            </ListView.Behaviors>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding TypeCheckListName}"
                              StyleId="disclosure"
                              Detail="{Binding TypeEnginName}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>

</ContentPage>
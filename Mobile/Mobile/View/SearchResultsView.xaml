<?xml version="1.0" encoding="utf-8" ?>
<Base:BaseView
    x:Class="Mobile.View.SearchResultsView"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:Base="clr-namespace:Mobile.View.Base"
    xmlns:vm="clr-namespace:Mobile.ViewModel">

    <ContentPage.Content>
        <StackLayout>
            <Frame
                Padding="10,15"
                BackgroundColor="White"
                HasShadow="True">
                <StackLayout
                    Orientation="Vertical"
                    Spacing="10"
                    VerticalOptions="Start">
                    <StackLayout>
                        <Label Text="{Binding ResultatExigence.DemandeAccesDto.TypeEnginName}" />
                    </StackLayout>

                </StackLayout>
            </Frame>
            <ListView ItemsSource="{Binding ResultatExigence.ResultatValueGroupingInfoG}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Label BackgroundColor="Yellow" Text="{Binding Key}" />

                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>

    </ContentPage.Content>
</Base:BaseView>